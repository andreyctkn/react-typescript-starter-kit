import React, { Component } from "react";

export default class App extends Component {
    public render(): JSX.Element {
        return (
            <div className="lat-page">
                <button>just added for test</button>
            </div>
        );
    }
}
